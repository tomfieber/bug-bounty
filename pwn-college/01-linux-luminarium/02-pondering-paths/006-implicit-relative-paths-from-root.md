This level explores relative paths. If we use absolute paths everywhere, then it doesn't really matter what directory we're in, but the `Current Working Directory (CWD)` **DOES** matter for relative paths.

>[!tip]
>For this level, the relative path starts with `c`.

From the root directory `/` run:

```bash
challenge/run
```

