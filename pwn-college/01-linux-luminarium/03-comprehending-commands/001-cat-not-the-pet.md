`cat` will con**cat**enate (hence the name) multiple files if provided multiple arguments. For example:

If you give no arguments at all, `cat` will read from the terminal input and output it. We'll explore that in later challenges...

```bash
# Run the challenge to write the flag to ~
/challenge/run

# Read the flag with cat
cat flag
```

